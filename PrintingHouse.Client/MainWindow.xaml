<Window x:Class="PrintingHouse.Client.MainWindow"
        WindowStartupLocation="CenterScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrintingHouse.Client.Properties"
        xmlns:vm="clr-namespace:PrintingHouse.Client.ViewModel"
        mc:Ignorable="d"
        Title="Printing House ERP" MinHeight="650" MinWidth="1000" Width="1000">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <TabControl Background="#D6DCE4" Name="tabControl" ItemsSource="{Binding Tabs}" SelectedIndex="1">
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Header" Value="{Binding Header}" />
                    <Setter Property="Content" Value="{Binding Content}" />
                </Style>
            </TabControl.ItemContainerStyle>
        </TabControl>

    </Grid>
</Window>
